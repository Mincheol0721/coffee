<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.spring.coffee.mapper.DailyBoardMapper">

    <insert id="insertDailyBoardImg" parameterType="list">
        <![CDATA[
        	INSERT INTO dailyBoard (fileName)
        	VALUES (StringArray(
        ]]>
        <foreach collection="fileList" item="fileName" separator=", " close="))">
        	#{fileName}
        </foreach>
    </insert>

</mapper>